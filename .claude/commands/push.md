Stage all modified files, create a commit with a descriptive message, and push to GitHub origin main.

Follow the standard git commit workflow:
1. Run `git status` and `git diff` to understand what changed.
2. Stage the relevant files with `git add`.
3. Write a clear commit message summarising the changes.
4. Commit and push to origin main.
